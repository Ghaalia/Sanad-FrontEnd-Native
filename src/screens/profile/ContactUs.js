import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ContactUs = () => {
  return (
    <View>
      <Text>ContactUs via </Text>
    </View>
  );
};

export default ContactUs;

const styles = StyleSheet.create({});
